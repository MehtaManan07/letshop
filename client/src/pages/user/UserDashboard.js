import React from "react";
import Layout from "../../components/Layout";
import { isAuth } from "../../functions/auth";
const UserDashboard = () => {
  return (
    <Layout title="User Dashboard" description="profile">
      ...
    </Layout>
  );
};

export default UserDashboard;
